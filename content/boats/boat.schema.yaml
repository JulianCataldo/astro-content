title: Boat
type: object
properties:
  heyWow:
    title: Hey wow
    type: object
    description: Wow this brand must be expensive
    required: [model, year]

    additionalProperties: false

    properties:
      model:
        type: string
        description: Wow this brand must be expensive
      year:
        type: number
        description: Not too old, not too new
  someFeatures:
    title: Some features!
    type: object
    description: Wow this brand must be expensive
    required: [model, year, color, brand]

    additionalProperties: false

    properties:
      model:
        type: string
        description: Wow this brand must be expensive
      year:
        type: number
        description: Not too old, not too new
      color:
        description: Peemp it
        type: string
        enum:
          - red
          - green
          - blue
      brand:
        description: Wiw it!!
        type: string
        enum:
          - dou
          - bi
          - douda
  theRichTextDesc:
    $ref: ../../schemas/markdown.schema.yaml
additionalProperties: false
