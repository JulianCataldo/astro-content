properties:
  main:
    allOf:
      - $ref: '#/definitions/MarkdownFile'
      - properties:
          frontmatter:
            properties:
              title:
                type: string
              order:
                type: number
required:
  - main
