properties:
  main:
    properties:
      title:
        type: string
      order:
        type: number

    required:
      - title
      - order

    additionalProperties: false

required:
  - main

additionalProperties: false
