properties:
  gallery:
    properties:
      public:
        type: array
        items:
          - type: string
    required:
      - public
  text:
    allOf:
      - $ref: '#/definitions/MarkdownFile'
  # - properties:
  #     frontmatter:
  #       properties:
  #         title:
  #           description: Entry title
  #           type: string
  #         image:
  #           description: Image path
  #           type: string
  #       required:
  #         - title
  #   required:
  #     - frontmatter

required:
  - text
  - gallery
