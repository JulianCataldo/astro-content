properties:
  bulma:
    allOf:
      - $ref: '#/definitions/MarkdownFile'
  inputText:
    allOf:
      - $ref: '#/definitions/MarkdownFile'
      - properties:
          frontmatter:
            properties:
              title:
                type: string
  image:
    allOf:
      - $ref: '#/definitions/MarkdownFile'
      - properties:
          frontmatter:
            properties:
              title:
                type: string
  kitchenSink:
    allOf:
      - $ref: '#/definitions/MarkdownFile'
      - properties:
          frontmatter:
            additionalProperties: false
            properties:
              title:
                type: string
