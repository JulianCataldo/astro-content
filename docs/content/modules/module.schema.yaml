properties:
  gallery:
    properties:
      source:
        type: string
    required:
      - source

  text: {}

  relations: {}

  install:
    properties:
      foo:
        type: string
      bar:
        type: string
    required:
      - foo
      - bar

required:
  - text
  # - gallery
