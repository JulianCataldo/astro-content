---
// NOTE: DISABLED FOR NOW, using Vite glob in TS.

// NOTE: (important)
// Is this a hack?…
// Yes.
//
// We need to trigger files re-transform when saving content in editor,
// for that `Astro.glob` have to be accessible, but we don't want loading
// all the layout / html stuff in `ssr-entrypoint` here.
// It might help to make this a bit snappier? It seems so.

// import { collect } from '@astro-content/server/collect';
// import { isValidContentBase } from '@astro-content/server/setup';

// const isValid = isValidContentBase();
// if (isValid) {
//   await collect(
//     //
//     Astro.glob('/content/**/*.{md,mdx,yaml}'),
//     { editMode: true },
//   );
// }
---
